import styles from '../../styles/layout/Navbar.module.scss';

const Navbar = () => {
    return(
        <nav className={styles.nav}>
            <ul>
                <li></li>
            </ul>
        </nav>
    )
}

export default Navbar;